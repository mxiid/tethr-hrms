# Copy to .env. Vite only exposes vars prefixed with VITE_ to the client.
VITE_GRAPHQL_URL=http://localhost:3000/graphql

# Error/performance monitoring. Optional — the app falls back to the shared
# dev project's DSN when this is unset.
VITE_SENTRY_DSN=
